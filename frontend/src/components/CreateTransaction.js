import React from 'react';
import { Form } from 'react-bootstrap';

const CreateTransaction = () => (
  //   const [inputs, setInputs] = React.useState({});
  <div className="module-box" id="create-transaction">
    <Form>
      <Form.Label>
        <Form.Control />
      </Form.Label>
    </Form>
  </div>
);
export default CreateTransaction;
